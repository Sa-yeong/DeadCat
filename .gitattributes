# 모든 텍스트 파일의 줄바꿈을 저장소에서 LF로 통일 (환경 차이 제거)
* text=auto eol=lf

# Windows 전용 스크립트만 CRLF 유지
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# 바이너리(줄바꿈 변환 금지)
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.pdf   binary
